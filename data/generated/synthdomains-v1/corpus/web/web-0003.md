# Web synthetic record 0003

The web crawl note states that anchor code WEB-0003 maps to site item 111 under the canonical-route signal.
A nearby but different web note references anchor code WEB-0004 for contrastive retrieval practice.
The record should be selected only when the query names WEB-0003 or repeats its exact mapped item 111.
