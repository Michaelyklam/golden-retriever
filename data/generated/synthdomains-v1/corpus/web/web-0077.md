# Web synthetic record 0077

The web crawl note states that anchor code WEB-0077 maps to site item 855 under the canonical-route signal.
A nearby but different web note references anchor code WEB-0078 for contrastive retrieval practice.
The record should be selected only when the query names WEB-0077 or repeats its exact mapped item 855.
