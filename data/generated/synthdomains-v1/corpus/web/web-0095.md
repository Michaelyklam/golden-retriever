# Web synthetic record 0095

The web crawl note states that anchor code WEB-0095 maps to site item 524 under the canonical-route signal.
A nearby but different web note references anchor code WEB-0096 for contrastive retrieval practice.
The record should be selected only when the query names WEB-0095 or repeats its exact mapped item 524.
