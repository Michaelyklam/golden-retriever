# Web synthetic record 0178

The web crawl note states that anchor code WEB-0178 maps to site item 604 under the canonical-route signal.
A nearby but different web note references anchor code WEB-0179 for contrastive retrieval practice.
The record should be selected only when the query names WEB-0178 or repeats its exact mapped item 604.
