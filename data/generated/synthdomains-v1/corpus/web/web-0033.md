# Web synthetic record 0033

The web crawl note states that anchor code WEB-0033 maps to site item 224 under the canonical-route signal.
A nearby but different web note references anchor code WEB-0034 for contrastive retrieval practice.
The record should be selected only when the query names WEB-0033 or repeats its exact mapped item 224.
