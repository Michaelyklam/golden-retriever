# Web synthetic record 0122

The web crawl note states that anchor code WEB-0122 maps to site item 526 under the canonical-route signal.
A nearby but different web note references anchor code WEB-0123 for contrastive retrieval practice.
The record should be selected only when the query names WEB-0122 or repeats its exact mapped item 526.
