# Web synthetic record 0081

The web crawl note states that anchor code WEB-0081 maps to site item 006 under the canonical-route signal.
A nearby but different web note references anchor code WEB-0082 for contrastive retrieval practice.
The record should be selected only when the query names WEB-0081 or repeats its exact mapped item 006.
