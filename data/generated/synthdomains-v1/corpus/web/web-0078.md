# Web synthetic record 0078

The web crawl note states that anchor code WEB-0078 maps to site item 892 under the canonical-route signal.
A nearby but different web note references anchor code WEB-0079 for contrastive retrieval practice.
The record should be selected only when the query names WEB-0078 or repeats its exact mapped item 892.
