# Web synthetic record 0023

The web crawl note states that anchor code WEB-0023 maps to site item 851 under the canonical-route signal.
A nearby but different web note references anchor code WEB-0024 for contrastive retrieval practice.
The record should be selected only when the query names WEB-0023 or repeats its exact mapped item 851.
