# Web synthetic record 0179

The web crawl note states that anchor code WEB-0179 maps to site item 641 under the canonical-route signal.
A nearby but different web note references anchor code WEB-0180 for contrastive retrieval practice.
The record should be selected only when the query names WEB-0179 or repeats its exact mapped item 641.
