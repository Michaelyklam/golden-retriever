# Web synthetic record 0148

The web crawl note states that anchor code WEB-0148 maps to site item 491 under the canonical-route signal.
A nearby but different web note references anchor code WEB-0149 for contrastive retrieval practice.
The record should be selected only when the query names WEB-0148 or repeats its exact mapped item 491.
