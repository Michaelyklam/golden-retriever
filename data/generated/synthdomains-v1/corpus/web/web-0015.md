# Web synthetic record 0015

The web crawl note states that anchor code WEB-0015 maps to site item 555 under the canonical-route signal.
A nearby but different web note references anchor code WEB-0016 for contrastive retrieval practice.
The record should be selected only when the query names WEB-0015 or repeats its exact mapped item 555.
