# Web synthetic record 0125

The web crawl note states that anchor code WEB-0125 maps to site item 637 under the canonical-route signal.
A nearby but different web note references anchor code WEB-0126 for contrastive retrieval practice.
The record should be selected only when the query names WEB-0125 or repeats its exact mapped item 637.
