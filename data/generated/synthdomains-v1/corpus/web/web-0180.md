# Web synthetic record 0180

The web crawl note states that anchor code WEB-0180 maps to site item 678 under the canonical-route signal.
A nearby but different web note references anchor code WEB-0181 for contrastive retrieval practice.
The record should be selected only when the query names WEB-0180 or repeats its exact mapped item 678.
