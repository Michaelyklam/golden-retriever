# Web synthetic record 0130

The web crawl note states that anchor code WEB-0130 maps to site item 822 under the canonical-route signal.
A nearby but different web note references anchor code WEB-0131 for contrastive retrieval practice.
The record should be selected only when the query names WEB-0130 or repeats its exact mapped item 822.
