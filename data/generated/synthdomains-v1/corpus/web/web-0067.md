# Web synthetic record 0067

The web crawl note states that anchor code WEB-0067 maps to site item 485 under the canonical-route signal.
A nearby but different web note references anchor code WEB-0068 for contrastive retrieval practice.
The record should be selected only when the query names WEB-0067 or repeats its exact mapped item 485.
