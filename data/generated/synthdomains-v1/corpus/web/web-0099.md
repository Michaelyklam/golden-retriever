# Web synthetic record 0099

The web crawl note states that anchor code WEB-0099 maps to site item 672 under the canonical-route signal.
A nearby but different web note references anchor code WEB-0100 for contrastive retrieval practice.
The record should be selected only when the query names WEB-0099 or repeats its exact mapped item 672.
