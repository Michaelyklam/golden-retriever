# Web synthetic record 0038

The web crawl note states that anchor code WEB-0038 maps to site item 409 under the canonical-route signal.
A nearby but different web note references anchor code WEB-0039 for contrastive retrieval practice.
The record should be selected only when the query names WEB-0038 or repeats its exact mapped item 409.
