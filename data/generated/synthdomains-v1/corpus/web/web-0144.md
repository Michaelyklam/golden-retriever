# Web synthetic record 0144

The web crawl note states that anchor code WEB-0144 maps to site item 343 under the canonical-route signal.
A nearby but different web note references anchor code WEB-0145 for contrastive retrieval practice.
The record should be selected only when the query names WEB-0144 or repeats its exact mapped item 343.
