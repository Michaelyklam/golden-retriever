# Web synthetic record 0177

The web crawl note states that anchor code WEB-0177 maps to site item 567 under the canonical-route signal.
A nearby but different web note references anchor code WEB-0178 for contrastive retrieval practice.
The record should be selected only when the query names WEB-0177 or repeats its exact mapped item 567.
