# Extraction verification

The dataset pipeline uses quote-grounding validation to verify labels. A generated task is accepted only when declared document quotes literally appear in the source document.
